# tests/e2e

Reserved for cross-service integration tests (frontend + API + database together) once there's a real user flow to exercise end-to-end. Phase 2's tests are unit-level and live next to the code they test: `apps/api/tests/` and `apps/web/tests/`.
