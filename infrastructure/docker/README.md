# infrastructure/docker

Reserved. `apps/api/Dockerfile` and `apps/web/Dockerfile` are sufficient for the Phase 2 foundation and live next to the code they build. This directory is for infrastructure-wide Docker assets (e.g. a shared base image, a reverse proxy config) if and when one is needed — not duplicated per-app Dockerfiles.
