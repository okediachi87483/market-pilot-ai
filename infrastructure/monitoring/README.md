# infrastructure/monitoring

Reserved for Phase 14 (observability). Prometheus scrape config and Grafana dashboard definitions land here — see [docs/observability.md](../../docs/observability.md). The `/metrics` endpoint they'd scrape doesn't exist yet; Phase 2 only ships `/health`, `/health/live`, and `/health/ready`.
