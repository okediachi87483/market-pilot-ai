import { ScreenPlaceholder } from "@/components/ui/ScreenPlaceholder";

export default function AlertsPage() {
  return (
    <ScreenPlaceholder description="Signal, risk-threshold, and profit-protection alerts. Arrives with the alerts package." />
  );
}
