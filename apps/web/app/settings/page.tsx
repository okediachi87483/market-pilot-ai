import { ScreenPlaceholder } from "@/components/ui/ScreenPlaceholder";

export default function SettingsPage() {
  return <ScreenPlaceholder description="Account, theme, and notification preferences. Arrives with authentication in a later phase." />;
}
