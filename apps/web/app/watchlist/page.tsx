import { ScreenPlaceholder } from "@/components/ui/ScreenPlaceholder";

export default function WatchlistPage() {
  return (
    <ScreenPlaceholder description="Manage tracked assets across one or more watchlists. Arrives with the market-data package." />
  );
}
